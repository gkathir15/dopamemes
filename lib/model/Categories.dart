class Categories{
  String id;
  String name;
  String PofilePic;
  int createdAt;
  int updatedAt;

  Categories(
      this.id, this.name, this.PofilePic, this.createdAt, this.updatedAt);

  Categories.name(this.name);
}